import 'package:flutter/material.dart';

class CheckForUpdatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('In CheckForUpdate page, you must wait for update is '
            'done before continuing to HomePage or MapPage'),
      ),
    );
  }
}
